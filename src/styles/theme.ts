import { DefaultTheme } from "styled-components";

const defaultColor = {
  black: "#000000",
  white: "#FFFFFF",
};

const colorPallte = {
  gray100: "#a2a4ab",
  gray200: "#989898",
};

const fontSize = {
  small: "14px",
  medium: "24px",
  large: "28px",
};

export const theme: DefaultTheme = {
  defaultColor,
  fontSize,
  colorPallte,
};
